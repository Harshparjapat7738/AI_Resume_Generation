package ai.careerforge.document.domain;

/** docs/DATABASE.md &sect;3 ({@code rendered_documents.documentType}). Only {@code RESUME}
 *  is produced today — cover-letter rendering is Milestone 8 (application-service) and out
 *  of scope here. */
public enum DocumentType {
    RESUME
}
