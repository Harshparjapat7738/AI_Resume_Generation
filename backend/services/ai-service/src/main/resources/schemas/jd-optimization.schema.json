{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "JD requirement adjudication output (ADR-038)",
  "description": "ID-referenced only, deliberately minimal (ADR-038): keywords, missingRequirements, emphasis and targetRole/targetCompany are computed deterministically downstream (jd-service) from this plus the JD analysis, never asked of the model — the one thing genuinely requiring judgement is whether the pre-selected evidence for a requirement actually demonstrates it.",
  "type": "object",
  "additionalProperties": false,
  "required": ["matches"],
  "properties": {
    "matches": {
      "type": "array",
      "maxItems": 25,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["requirementId", "evidenceIds", "matchKind"],
        "properties": {
          "requirementId": { "type": "string", "pattern": "^REQ-[0-9]{3,4}$" },
          "evidenceIds": {
            "type": "array",
            "maxItems": 3,
            "items": { "type": "string", "pattern": "^(EXP|PROJ|SKILL|CERT|EDU|ACH)-[0-9]{3,4}$" }
          },
          "confidence": { "type": ["number", "null"], "minimum": 0, "maximum": 1 },
          "matchKind": { "type": "string", "enum": ["STRONG", "PARTIAL", "NONE"] }
        }
      }
    }
  }
}
