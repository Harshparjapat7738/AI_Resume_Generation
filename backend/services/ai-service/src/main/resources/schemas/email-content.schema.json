{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Application email content output",
  "type": "object",
  "additionalProperties": false,
  "required": ["greeting", "bodyParagraph", "closingParagraph", "signOff"],
  "$defs": {
    "evidenceId": {
      "type": "string",
      "pattern": "^(EXP|PROJ|SKILL|CERT|EDU|ACH)-[0-9]{3,4}$"
    },
    "groundedText": {
      "type": "object",
      "additionalProperties": false,
      "required": ["text", "evidenceIds"],
      "properties": {
        "text": { "type": "string", "minLength": 1, "maxLength": 800 },
        "evidenceIds": {
          "type": "array",
          "minItems": 1,
          "maxItems": 10,
          "items": { "$ref": "#/$defs/evidenceId" }
        }
      }
    }
  },
  "properties": {
    "greeting": { "type": "string", "minLength": 1, "maxLength": 200 },
    "bodyParagraph": { "$ref": "#/$defs/groundedText" },
    "closingParagraph": { "$ref": "#/$defs/groundedText" },
    "signOff": { "type": "string", "minLength": 1, "maxLength": 100 }
  }
}
