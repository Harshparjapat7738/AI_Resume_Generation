package ai.careerforge.application.document;

/** Physical page size {@code render-service} lays the document out on (ADR-036). A rendering
 *  knob, not content — see {@link RenderHints}. */
public enum PageSize {
    A4,
    LETTER
}
